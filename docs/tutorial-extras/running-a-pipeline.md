---
sidebar_position: 3
---

# Running a Pipeline

Once you have configured your pipeline, you can run it using the `dagger run` command:

```bash
fluentci run .
```

Or just:

```bash
fluentci run .
```

Or simply:

```bash
fluentci
```